function Board( {takes_in_latest_move} ) {
  return ( 
      //appends latest move coordinates to an array making the history 

      //after 2n moves deletes earliest move

      
   );
}

export default Board;